# Data Srorage  -> to store user info 

# DataBase Types:
SQL,NoSql

SQL -> in tabular format(rows and col)
NoSql -> object format 

# MongoDb -> NOSql based database 

{
    age:25,
    name:"anuja"
}

# backend -> application server(node) & database server (mongoDb)
application server -> handle routes ,request,response,login ,logout etc 
database server -> deals  with data request 

database-> colections->Document
# Terminologies 
1. Collections :identities under a database
2. Document : a single entity data

moongose.connect => database gets  created 
model create => collection gets created
CREATE        => document got created 

# installation 
1. MongoDb -> instal communit edition 


# Routes 
Routing in Express.js is the process of mapping incoming HTTP requests (defined by method and URL) to specific handler functions
app.METHOD(PATH, HANDLER);


# Schemas 
import moongoose

